import BusinessInformation from '@/components/auth/form/BusinessInformation'
import React from 'react'

type Props = {}

const page = (props: Props) => {
   return <BusinessInformation />
}

export default page
